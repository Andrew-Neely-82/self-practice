"use strict";
// ? set the current dir by "cd siteRecreation/githubProfile"
// ? to compile run => "tsc -p githubProfile"
(() => {
  //
  $(`.search`).click((e) => {
    e.preventDefault;
  });
})();
